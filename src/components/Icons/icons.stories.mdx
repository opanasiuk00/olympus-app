import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';
import { ICON_OPTIONS } from './storybook';

<Meta title='UI/Iconography' />

# Iconography

<IconGallery>
	{Object.entries(ICON_OPTIONS).map(([iconName, iconComponent]) => (
		<IconItem
			name={iconName}
			key={iconName}
		>
			{iconComponent}
		</IconItem>
	))}
</IconGallery>
